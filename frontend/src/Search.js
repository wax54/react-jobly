import React from "react";

const Search = ({ list, updateList}) => (
    <div className="Search">
        Hi Im the Search Component
        <input type="text" name="search"/> 
    </div>
);
export default Search